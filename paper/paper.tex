\documentclass[article]{ajs}

\usepackage{amsmath}
\usepackage{float}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% declarations for ajs.cls %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\author{Arquímedes Chacón\,\orcidlink{0000-0002-7014-7513}\\ Universidad Católica Andrés Bello, Venezuela}

%% WORKING TITLE -- candidates discussed, easy to swap later:
%%  1. Stress-Testing Chatterjee's Xi Correlation Coefficient: A Monte Carlo
%%     Study of Sample Size, Distribution Shape, and Variable Type
%%  2. A Factorial Monte Carlo Evaluation of Chatterjee's Xi Correlation
%%     Coefficient Against Established Measures of Dependence
%%  3. Chatterjee's Xi Correlation Coefficient Under Stress: Sample Size,
%%     Distribution Shape, and Variable Type Effects in a Monte Carlo
%%     Framework
\title{Stress-Testing Chatterjee's Xi Correlation Coefficient: A Monte Carlo Study of Sample Size, Distribution Shape, and Variable Type}

\Plainauthor{Arquímedes Chacón}
\Plaintitle{Stress-Testing Chatterjee's Xi Correlation Coefficient: A Monte Carlo Study of Sample Size, Distribution Shape, and Variable Type}
\Shorttitle{Stress-Testing Chatterjee's Xi Correlation Coefficient}

\Abstract{
  % NOTA: redactado en español para revision; traducir al ingles en
  % la version final antes de enviar.
  El coeficiente de correlación $\xi$ de Chatterjee (2021) promete
  detectar cualquier forma de dependencia funcional entre dos
  variables mediante una fórmula basada en rangos, con una prueba de
  independencia asintótica computacionalmente barata. Pese al
  rápido crecimiento de la literatura teórica sobre $\xi_n$ desde su
  introducción, ningún estudio previo lo ha sometido a un diseño de
  simulación Monte Carlo que cruce sistemáticamente tamaño de
  muestra, tipo de variable, forma de la distribución, y geometría de
  la dependencia, ni lo ha comparado bajo esas mismas condiciones
  contra distance correlation y medidas basadas en cópulas. Este
  artículo cierra ese vacío mediante un diseño por módulos, cada uno
  fundamentado en una propiedad ya demostrada de $\xi_n$, motivado
  por la investigación en psicología, donde los constructos son
  fenómenos multifactoriales difíciles de aislar. Los resultados
  confirman un sesgo de muestra finita que decrece con $n$ pero no
  depende únicamente de la cota algebraica conocida; costos distintos
  y no aditivos de los empates según afecten al predictor, a la
  respuesta, o a ambos, atenuables mediante puntajes compuestos con
  rendimientos marcadamente decrecientes; una comparación con distance
  correlation ante cambios de familia marginal que resultó más matizada
  de lo esperado --- $\xi_n$ más estable en el valor puntual estimado,
  pero no uniformemente en sesgo o varianza --- excepto en un
  sub-experimento aislado donde la invariancia de $\xi_n$ ante
  transformaciones monótonas se sostuvo sin matices; una
  geometría de la dependencia que determina qué estadístico detecta
  mejor --- con un hallazgo inesperado en que distance correlation y
  una medida basada en cópulas divergen sustancialmente entre sí en
  relaciones circulares, en vez de comportarse como un bloque
  homogéneo de alternativas; y una asimetría empírica nítida entre el
  umbral de detección subóptimo de $\xi_n$ cerca de independencia y su
  optimalidad lejos de ella. Una ilustración con datos reales de
  personalidad reproduce dos de estos hallazgos directamente. En
  conjunto, los resultados ofrecen una guía práctica sobre cuándo
  $\xi_n$ debe preferirse, interpretarse con cautela, o complementarse
  con medidas clásicas, en datos psicológicos reales.
}

\Keywords{chatterjee's xi, rank correlation, monte carlo simulation, measures of dependence, distance correlation, robustness, \proglang{R}}
\Plainkeywords{chatterjee's xi, rank correlation, monte carlo simulation, measures of dependence, distance correlation, robustness, R}

%% publication information
%% NOTE: left commented -- filled out by the technical editor
%% \Volume{}
%% \Issue{}
%% \Month{}
%% \Year{}
%% \Submitdate{}
%% \Acceptdate{}
\Pages{1--xx}

\Address{
  Arquímedes Chacón\\
  Universidad Católica Andrés Bello\\
  Venezuela\\
  E-mail: \email{arquimedeschacon@gmail.com}
}

%% end of declarations %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

\section{Introducción} \label{sec:introduction}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles en la version final antes de enviar.

Cuantificar la fuerza de una relación entre dos variables es una de
las tareas más antiguas y más recurrentes de la estadística aplicada.
Sin embargo, los tres coeficientes que dominan la práctica ---
Pearson, Spearman, y Kendall--- comparten una limitación conocida:
están diseñados para detectar dependencia lineal o, a lo sumo,
monótona, y pueden acercarse a cero incluso ante una relación
funcional perfecta y sin ruido si esta no es monótona
\citep{chatterjee2021}. En ciencias del comportamiento y sociales,
donde las relaciones entre constructos rara vez se conocen de
antemano y a menudo involucran umbrales, techos, o formas en U, esta
limitación no es un tecnicismo menor.

Este artículo busca aportar específicamente al ámbito de la
psicología, donde esta limitación adquiere una relevancia particular.
Los constructos psicológicos --- personalidad, actitudes, síntomas,
cogniciones --- rara vez son el resultado de un mecanismo aislable:
emergen de fenómenos multifactoriales en los que intervienen
simultáneamente disposiciones individuales, historia de aprendizaje,
contexto social, y procesos biológicos, ninguno de los cuales puede
controlarse o aislarse experimentalmente con la limpieza de un sistema
físico. Esta naturaleza multifactorial implica que la forma funcional
de la relación entre dos variables psicológicas rara vez se conoce de
antemano, de modo que asumir linealidad o monotonía no es una
simplificación inocua sino una apuesta sobre un mecanismo que, por su
propia naturaleza, resulta difícil de aislar y verificar. Un
coeficiente que promete detectar cualquier forma de dependencia
funcional, como $\xi_n$, es en principio la herramienta natural para
esta situación --- pero esa misma promesa exige someterlo a las
condiciones bajo las cuales efectivamente se recogen los datos
psicológicos: escalas ordinales de pocos puntos (ítems tipo Likert),
tamaños de muestra moderados por costos de reclutamiento, y
poblaciones heterogéneas que producen distribuciones asimétricas o
mixturas. Es exactamente esa combinación de condiciones la que este
estudio somete a prueba, y la que se ilustra en la
Sección~\ref{sec:realdata} con datos reales de personalidad.

Esta ceguera ante la posible no monotonía no se limita a los
coeficientes de correlación bivariados: incluso cuando la práctica
psicométrica estándar --- modelos de ecuaciones estructurales,
estructuras factoriales de segundo orden --- se topa con relaciones
inesperadas que obligan a reespecificar el modelo (cargas cruzadas,
residuos correlacionados, factores adicionales), esa reespecificación
ocurre casi siempre dentro del mismo paradigma de relaciones
monótonas o lineales entre factores e indicadores: rara vez se
cuestiona si la relación subyacente podría no ser monótona en
absoluto. Explorar sistemáticamente esa posibilidad --- por ejemplo,
mediante análisis de ruta simulados con coeficientes verdaderamente no
monótonos --- excede el alcance de este artículo y se deja como línea
de trabajo futura; aquí nos concentramos en el caso bivariado.

\citet{chatterjee2021} propuso una alternativa que promete resolver
este problema con una fórmula casi trivial: un coeficiente basado en
rangos, $\xi_n$, que converge a una cantidad poblacional igual a cero
si y solo si las variables son independientes, e igual a uno si y
solo si una es función medible de la otra --- sin importar la forma
de esa función --- y que además admite una prueba de independencia
con distribución asintótica conocida, sin necesidad de remuestreo.
Esta combinación de simplicidad, generalidad, y tratabilidad
asintótica explica por qué, en apenas unos años, $\xi_n$ ha generado
una literatura teórica en rápido crecimiento: extensiones a
dependencia condicional y multivariante \citep{azadkia2021},
correcciones de sesgo \citep{dalitz2024}, familias generalizadas
\citep{gao2024}, resultados sobre continuidad \citep{ansari2026,
bucher2025}, y umbrales exactos de detección \citep{auddy2024}.

Pese a ese volumen de trabajo teórico, ninguno de esos estudios --- ni
ningún otro que hayamos podido localizar --- somete a $\xi_n$ a un
diseño de simulación factorial que cruce sistemáticamente tamaño de
muestra, tipo de variable, forma de la distribución, estructura de
dependencia, y magnitud de la dependencia, ni lo compara bajo esas
mismas condiciones contra los puntos de referencia históricos de la
dependencia no lineal (distance correlation, medidas basadas en
cópulas). Las simulaciones existentes son, en el mejor de los casos, de alcance
muy acotado: unos pocos modelos fijos, uno o dos tamaños de muestra,
sin variar el tipo de variable. Este artículo llena ese
vacío --- pero, como se detalla en la Sección~\ref{sec:framework}, no
como un barrido exploratorio sin rumbo: cada uno de los cinco
factores del diseño se eligió porque la propia estructura algebraica
o asintótica de $\xi_n$, ya demostrada en la literatura citada arriba,
predice o al menos permite un efecto específico sobre su
comportamiento. Formulamos esas predicciones como cinco hipótesis
(H1--H5) antes de describir el diseño de simulación que las pone a
prueba.

Es importante precisar el alcance de este estudio desde ya.
\citet{bucher2025} demuestran, mediante construcciones adversariales
(``shuffles of min''), que $\xi$ puede tener potencia trivial y que no
existen intervalos de confianza significativos de nivel exacto para
$\xi$ en el peor de los casos. Nuestro estudio no busca refutar ni
replicar esos resultados de peor caso --- los damos por probados y
fuera de discusión--- sino cuantificar, mediante simulación, el
comportamiento de $\xi_n$ bajo condiciones \emph{típicas}, no
adversariales, del tipo que efectivamente encuentra un investigador
aplicado: distribuciones asimétricas o de colas pesadas, variables
ordinales tipo Likert, tamaños de muestra moderados, y estructuras de
dependencia realistas.

El resto del artículo se organiza así. La Sección~\ref{sec:framework}
presenta el marco teórico de $\xi_n$, sus medidas competidoras, y las
cinco hipótesis del estudio. La Sección~\ref{sec:design} describe el
diseño factorial de la simulación, los mecanismos generadores de
datos, y las métricas de resultado. La Sección~\ref{sec:results}
reporta los resultados organizados por hipótesis. La
Sección~\ref{sec:realdata} ilustra los hallazgos aplicando $\xi_n$ y
sus competidores a un conjunto de datos real de psicología. Las
Secciones~\ref{sec:discussion} y~\ref{sec:conclusion} discuten las
implicaciones prácticas para investigadores aplicados y concluyen.

\section{Marco teórico} \label{sec:framework}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles (y a title/sentence case segun estilo AJS)
% en la version final antes de enviar.

\subsection{El coeficiente xi de Chatterjee}

Las tres medidas clásicas de asociación --- $r$ de Pearson, $\rho$ de
Spearman y $\tau$ de Kendall --- son efectivas para detectar
dependencia lineal o monótona, pero pueden acercarse a cero incluso
ante una relación funcional perfecta, sin ruido, pero no monótona
entre $X$ e $Y$ \citep{chatterjee2021}. \cite{chatterjee2021} propuso
un coeficiente basado en rangos que supera esta restricción,
conservando la simplicidad y la tratabilidad asintótica de los
coeficientes clásicos.

Sean $(X_1,Y_1), \dots, (X_n,Y_n)$ copias i.i.d.\ de $(X,Y)$, con $Y$
no constante casi seguramente. Reordenamos los pares de modo que
$X_{(1)} \le \cdots \le X_{(n)}$ (los empates entre los $X_i$ se
rompen al azar), sea $r_i$ el rango de $Y_{(i)}$ entre todos los
$Y_j$, y sea $l_i$ el número de $j$ tales que $Y_j \ge Y_{(i)}$. El
coeficiente de correlación de Chatterjee se define como
\begin{equation} \label{eq:xi}
  \xi_n(X,Y) := 1 - \frac{n \sum_{i=1}^{n-1} |r_{i+1} - r_i|}
                         {2 \sum_{i=1}^{n} l_i (n - l_i)},
\end{equation}
que, en ausencia de empates entre los $Y_i$, se simplifica a
$\xi_n(X,Y) = 1 - 3\sum_{i=1}^{n-1}|r_{i+1}-r_i| / (n^2-1)$.

\cite{chatterjee2021} demuestra que $\xi_n(X,Y)$ converge casi
seguramente, cuando $n \to \infty$, a un límite determinístico
\begin{equation} \label{eq:xi-limit}
  \xi(X,Y) := \frac{\int \mathrm{Var}\big(E(\mathbf{1}_{\{Y \ge t\}} \mid X)\big)\, d\mu(t)}
                    {\int \mathrm{Var}\big(\mathbf{1}_{\{Y \ge t\}}\big)\, d\mu(t)},
\end{equation}
donde $\mu$ es la ley de $Y$. El límite satisface $\xi(X,Y) \in
[0,1]$, con $\xi(X,Y) = 0$ si y solo si $X$ e $Y$ son independientes,
y $\xi(X,Y) = 1$ si y solo si existe una función medible $f$ tal que
$Y = f(X)$ casi seguramente --- sin importar si $f$ es lineal,
monótona, u oscilante. No se requiere ningún supuesto distribucional
sobre $(X,Y)$, y $\xi_n$ es computable en tiempo $O(n \log n)$.

Tres propiedades de $\xi_n$ son directamente relevantes para el
diseño del estudio de simulación que sigue.

\emph{Direccionalidad.} A diferencia de los coeficientes de Pearson,
Spearman o Kendall, $\xi_n$ no es simétrico en $X$ e $Y$:
$\xi_n(X,Y) \ne \xi_n(Y,X)$ en general. Esto es intencional ---
$\xi_n(X,Y)$ cuantifica qué tan bien $Y$ está determinada por $X$, no
una asociación mutua y simétrica \citep{chatterjee2021}.

\emph{Sesgo de muestra finita.} Aunque $\xi_n$ converge a $\xi \in
[0,1]$, su valor máximo alcanzable para $n$ finito es estrictamente
menor que uno. \cite{dalitz2024} demuestran que, en ausencia de
empates entre los $Y_i$, $\xi_n(\vec x, \vec y) \le (n-2)/(n+1)$ para
toda permutación $\vec x$ del covariable --- para $n=20$ esta cota es
de solo $\approx 0.86$. Como esta cota es consecuencia de la
estructura algebraica de \eqref{eq:xi} y no del ruido en los datos,
garantiza un sesgo sistemático a la baja de $\xi_n$ como estimador de
$\xi$ cada vez que $\xi$ está cerca de uno, un efecto que debería
reducirse a medida que $n$ crece.

\emph{Varianza dependiente de la distribución bajo empates.} Bajo
independencia de $X$ e $Y$, $\sqrt{n}\,\xi_n(X,Y)$ es asintóticamente
normal con media cero \citep{chatterjee2021}. Si $Y$ es continua, la
varianza límite es una constante universal, $2/5$, libre de la ley de
$Y$ (Teorema 2.1). Si $Y$ \emph{no} es continua, en cambio, la
varianza límite $\tau^2$ depende de la ley de $Y$ (Teorema 2.2);
Chatterjee calcula $\tau^2 = 1$ para $Y \sim
\mathrm{Bernoulli}(1/2)$, exactamente 2.5 veces el valor del caso
continuo. Más aún, cuando los $X_i$ contienen empates, $\xi_n(X,Y)$
es en sí mismo un estadístico aleatorizado, ya que su valor depende
de cómo se rompan los empates. Estas no son curiosidades empíricas
sino consecuencias demostradas de \eqref{eq:xi}, y afectan
directamente a las variables discretas y ordinales del tipo que se
analiza rutinariamente en ciencias del comportamiento y sociales.

\subsection{Medidas competidoras de dependencia no lineal}

Otras dos familias de estadísticos han perseguido históricamente el
mismo objetivo --- detectar dependencia más allá de la linealidad o
la monotonía --- por vías matemáticas distintas, y son los puntos de
comparación naturales frente a los cuales debe evaluarse $\xi_n$.

\emph{Distance correlation.} \cite{szekely2007} definieron la
covarianza de distancias y la distance correlation (dCor) a partir de
las distancias par a par dentro de cada muestra, mostrando que
dCor$(X,Y) = 0$ si y solo si $X$ e $Y$ son independientes, para
vectores aleatorios $X$ e $Y$ de dimensión arbitraria. dCor es
simétrica y multivariante por construcción, pero, al operar sobre
distancias crudas en vez de rangos, no es invariante ante
reparametrizaciones monótonas no lineales de las marginales de la
forma en que sí lo son $\xi_n$, $\rho$ de Spearman o $\tau$ de
Kendall. Su costo computacional ingenuo es $O(n^2)$ (existe un
algoritmo rápido $O(n \log n)$ solo para el caso univariante), y la
prueba de independencia generalmente requiere un test de permutación
en vez de una distribución nula tratable.

\emph{Medidas basadas en cópulas.} \cite{hoeffding1948} propuso una de
las primeras pruebas no paramétricas generales de independencia, un
$U$-estadístico basado en rangos (la $D$ de Hoeffding) que mide la
discrepancia entre la distribución empírica conjunta de $(X,Y)$ y el
producto de sus marginales. $D$ es simétrica y consistente frente a
alternativas amplias, pero tiene un punto ciego conocido: ciertas
configuraciones dependientes y simétricas (por ejemplo, relaciones
tipo $Y = |X|$) pueden llevar a $D$ cerca de cero pese a existir
dependencia genuina. \cite{bergsma2014} introdujeron $\tau^\ast$, un
estadístico relacionado de covarianza de signos diseñado para ser
estrictamente positivo siempre que $X$ e $Y$ sean dependientes,
corrigiendo esta deficiencia. Ambos estadísticos son
$U$-estadísticos de orden cuatro o cinco, con un costo computacional
ingenuo de $O(n^2)$ o peor.

La Tabla~\ref{tab:comparison} resume las propiedades que distinguen a
$\xi_n$ de estas dos familias y que motivan su inclusión como puntos
de comparación en el diseño de simulación (Sección~\ref{sec:design}).

\begin{table}[H]
\caption{\label{tab:comparison}Propiedades de $\xi_n$ y sus medidas de dependencia de referencia.}
\begin{center}
\small
\begin{tabular}{p{4.2cm}p{1.6cm}p{2.1cm}p{2.6cm}}
\toprule
Propiedad & $\xi_n$ & dCor & $D$ Hoeffding / $\tau^\ast$ \\
\midrule
Simétrico en $(X,Y)$ & No & Sí & Sí \\[2pt]
Basado en rangos / invariante a monótonas & Sí & No & Sí \\[2pt]
Soporte multivariante nativo & No\footnotemark & Sí & No \\[2pt]
Costo computacional & $O(n \log n)$ & $O(n^2)$\footnotemark & $O(n^2)$ o peor \\[2pt]
Distribución nula tratable & Sí (Normal) & No (permutación) & No (permutación) \\
\bottomrule
\end{tabular}
\end{center}
\end{table}
\footnotetext[1]{\cite{azadkia2021} extienden $\xi_n$ a entornos condicionales y multivariantes; este estudio se limita al coeficiente bivariante original.}
\footnotetext[2]{Un algoritmo rápido $O(n\log n)$ para dCor existe solo en el caso univariante.}

\subsection{Efectos hipotetizados del tamaño de muestra, tipo de variable, forma de la distribución, estructura y régimen de dependencia}

El diseño de simulación de la Sección~\ref{sec:design} no es un
barrido sin guía de condiciones arbitrarias: cada hipótesis se eligió
porque la estructura algebraica o asintótica de $\xi_n$ revisada
arriba predice, o al menos permite, un efecto específico sobre su
comportamiento. Varias de las hipótesis originalmente planteadas como
un único efecto resultaron, tras un escrutinio más cuidadoso,
confundir mecanismos distintos que requieren diseños y estimandos
separados; se presentan aquí ya desagregadas.

\begin{description}
\item[H1 (tamaño de muestra).] La cota superior de muestra finita
  $(n-2)/(n+1)$ (Sección 2.1) demuestra, de forma puramente algebraica,
  que $\xi_n$ \emph{no puede} aproximarse a uno en muestras pequeñas
  cuando la dependencia poblacional real es alta --- pero esa cota no
  implica, por sí sola, que el sesgo sea sustancial para cualquier
  valor de $\xi$, ni que su patrón sea el mismo bajo toda estructura de
  dependencia: para $\xi \approx 0.5$, por ejemplo, la cota de $0.86$
  en $n=20$ no restringe de forma directa al estimador. Formulamos por
  tanto H1 como una hipótesis condicional: el sesgo negativo y el MSE
  de $\xi_n$ disminuirán al aumentar $n$, con una reducción
  particularmente marcada cuando el valor poblacional de $\xi$ sea
  alto; la magnitud --- y el signo --- del sesgo residual en otros
  regímenes deberá verificarse empíricamente y podrá depender de la
  estructura de dependencia y del patrón de empates (H2, H4). La
  interacción entre $n$ y el nivel de dependencia real es, así, parte
  sustantiva de H1 y no solo de H5.
\item[H2 (empates y tipo de variable).] La formulación original agrupaba
  bajo una sola etiqueta dos mecanismos demostrados que son, en
  realidad, preguntas distintas: los empates en $Y$ alteran la
  varianza asintótica de $\xi_n$ ($\tau^2$ depende de la ley de $Y$
  cuando esta no es continua, Sección 2.1), mientras que los empates en
  $X$ hacen de $\xi_n$ un estadístico aleatorizado por el desempate.
  Se desagregan en cuatro sub-hipótesis:
  \begin{description}
  \item[H2a (empates en la respuesta).] Manteniendo $X$ continua,
    discretizar $Y$ incrementará o alterará la variabilidad de $\xi_n$
    y podrá modificar el desempeño del test de independencia respecto
    al caso continuo.
  \item[H2b (empates en el predictor).] Manteniendo $Y$ continua,
    discretizar $X$ introducirá variabilidad adicional atribuible al
    desempate aleatorio y reducirá la reproducibilidad de $\xi_n$; este
    efecto será mayor con menos categorías y con muestras pequeñas.
  \item[H2c (discretización conjunta).] Cuando ambas marginales sean
    discretas, el comportamiento de $\xi_n$ reflejará la combinación de
    ambos mecanismos y no será recuperable por simple extrapolación de
    H2a y H2b.
  \item[H2d (puntaje compuesto).] En la práctica psicométrica real, un
    constructo rara vez se mide con un único ítem: se agregan varios
    ítems (tipo Likert) que lo miden con error de medición
    independiente en un puntaje compuesto (p.\ ej.\ el promedio). Se
    espera que agregar $k$ ítems atenúe sustancialmente el problema de
    empates documentado en H2a--H2c respecto a un único ítem crudo,
    aunque sin eliminarlo, dado que el puntaje compuesto conserva un
    número finito de valores posibles.
  \end{description}
  Cabe además una precisión de interpretación: que la discretización
  ``preserve la estructura de dependencia subyacente'' es válido
  respecto al mecanismo latente, pero no implica que preserve el mismo
  valor observado de $\xi$ --- al discretizar se transforma la
  distribución conjunta observada, y con ella puede cambiar $\xi$, lo
  que es particularmente relevante dado el carácter direccional de
  $\xi_n$.
\item[H3 (forma de la distribución).] La invariancia de $\xi_n$ ante
  transformaciones estrictamente monótonas de las marginales sugeriría,
  en principio, estabilidad frente a la forma de la distribución. Sin
  embargo, \cite{ansari2026} demuestran que $\xi$ \emph{no} es continua
  respecto a convergencia débil en general, y que su continuidad solo
  ha sido establecida para familias específicas (elípticas,
  $\ell_1$-simétricas); \cite{bucher2025} llevan esto al extremo con
  construcciones adversariales (``shuffles of min'') que quedan fuera
  del alcance de este estudio (Sección~\ref{sec:introduction}). Sin
  embargo, variar la familia marginal dentro de un modelo aditivo
  $Y=f(X)+\sigma E$ no aísla limpiamente el efecto de la forma
  marginal: también cambia la distribución del error, la cópula
  inducida entre $X$ e $Y$, el valor poblacional de $\xi$ antes y
  después de calibrar, y la dificultad relativa del problema para dCor.
  Reformulamos H3 en consecuencia: tras calibrar cada condición a un
  mismo nivel poblacional de dependencia direccional (Sección
  \ref{sec:design}), se evaluará si el sesgo, la varianza, y la
  potencia de $\xi_n$ cambian sistemáticamente entre familias
  marginales; las diferencias se interpretarán como sensibilidad al
  \emph{mecanismo generador conjunto} asociado a cada familia, no
  exclusivamente como sensibilidad a la marginal. Distinguimos además,
  dentro de H3, dos ejercicios formalmente separados: (i) una
  \emph{validación} con las familias elípticas (normal, $t$) para las
  que \citet{ansari2026} ya proveen fórmula cerrada --- si la
  simulación no la reproduce, no se avanza a (ii); y (ii) un
  \emph{estudio de robustez empírica} con las familias no elípticas
  del diseño (exponencial, mixtura bimodal), donde reside el vacío
  genuino. Por último, dado que la comparación con dCor busca ilustrar
  una diferencia de invariancia (rangos vs.\ distancias crudas), y que
  cambiar de familia marginal mediante un nuevo modelo aditivo
  \emph{no} equivale a aplicar una transformación monótona al mismo par
  $(X,Y)$, esa propiedad se aísla mejor en un sub-experimento
  específico (Sección~\ref{sec:design}) que en el factorial principal.
\item[H4 (geometría de la dependencia).] \cite{chatterjee2021} y
  \cite{gao2024} ya reportan que $\xi_n$ es más potente que dCor para
  relaciones oscilatorias y no monótonas, pero menos potente para
  relaciones lineales, circulares o heterocedásticas. Sin embargo, estas
  cinco estructuras no representan variaciones de un mismo tipo de
  dependencia: lineal, cuadrática, y senoidal tienen un componente
  funcional direccional de $X$ hacia $Y$; la circular no es una función
  global unívoca de $X$; y la heterocedástica exhibe dependencia
  condicional (en la dispersión) que puede ser fuerte incluso cuando
  las medidas clásicas de asociación son pequeñas. Reformulamos H4
  como una hipótesis sobre \emph{geometría}, no solo forma funcional:
  el orden relativo de potencia entre $\xi_n$, dCor, y los estadísticos
  basados en cópulas dependerá de la geometría de la dependencia ---
  $\xi_n$ debería ser especialmente competitivo en relaciones
  direccionales, no monótonas, y funcionalmente informativas de $X$
  hacia $Y$; dCor debería mantener ventaja en configuraciones
  simétricas, circulares, o dominadas por dispersión condicional. Esta
  comparación de potencia solo es interpretable si los tests
  comparados tienen error de tipo I empírico comparable al nivel
  nominal bajo cada condición (Sección~\ref{sec:design}, 3.3); de lo
  contrario, una ``mayor potencia'' puede ser únicamente un artefacto
  de calibración.
\item[H5 (régimen de dependencia).] La formulación original mezclaba
  dos preguntas distintas. \cite{auddy2024} demuestran que, bajo
  \emph{alternativas locales} --- secuencias en las que la magnitud de
  la señal decrece con $n$ a una tasa específica ---, el umbral de
  detección de $\xi_n$ es subóptimo ($n^{-1/4}$) para testear
  independencia pura ($\xi_0=0$) pero minimax óptimo ($n^{-1/2}$) para
  testear grado de asociación ($\xi_0>0$). Un diseño con niveles
  \emph{fijos} de $\xi_0$ a través de todos los tamaños de muestra ---
  como el originalmente propuesto --- es útil para estudiar sesgo, MSE,
  y curvas de potencia prácticas, pero no prueba directamente ese
  resultado sobre alternativas locales, para el cual la señal debe
  depender explícitamente de $n$. Se distinguen, en consecuencia, dos
  sub-hipótesis:
  \begin{description}
  \item[H5a (desempeño práctico a dependencia fija).] A valores
    poblacionales fijos de $\xi_0 \in \{0.1,0.3,0.5,0.8\}$, la potencia
    aumentará con $n$, pero el ritmo de aumento dependerá de la
    estructura de dependencia (H4) y de la presencia de empates (H2).
  \item[H5b (sensibilidad local).] Bajo una secuencia de alternativas
    cuya señal decrece al crecer $n$ (siguiendo el marco de
    \citealp{auddy2024}), $\xi_n$ mostrará un umbral de detección
    distinto al de sus competidores, especialmente cerca de
    independencia.
  \end{description}
  Para H5a, el nivel $\xi_0 \approx 0$ requiere una condición explícita
  de independencia exacta ($X \perp Y$ generada directamente), distinta
  de la ``casi independencia'' con señal decreciente que se reserva
  para H5b --- un $\sigma$ simplemente muy grande no equivale a
  independencia exacta para efectos de evaluar error de tipo I.
\end{description}

\section{Diseño de simulación} \label{sec:design}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles en la version final antes de enviar.

\subsection{Diseño por módulos}

Un factorial completo sobre los cinco ejes de la Sección~\ref{sec:framework}
($n$, tipo de variable, forma marginal, estructura, régimen) --- y sus
factores adicionales no siempre explícitos, como la marginal
discretizada (H2a/b/c) o la direccionalidad $\xi_n(X,Y)$ vs.\
$\xi_n(Y,X)$ --- resultaría en una matriz de miles de celdas,
computacionalmente inmanejable dado el costo $O(n^2)$ o peor de los
competidores (Sección~\ref{sec:design}, 3.4) y, más importante,
innecesaria: cada hipótesis requiere que varíen solo uno o dos
factores, no todos a la vez. En vez de un factorial completo reducido
después por motivos computacionales, el diseño se estructura desde el
inicio como módulos dirigidos por hipótesis, cada uno con su propia
pregunta y sus propios factores:

\begin{table}[H]
\caption{\label{tab:modules}Módulos del diseño de simulación.}
\begin{center}
\small
\begin{tabular}{p{1.6cm}p{4.6cm}p{5.3cm}}
\toprule
Módulo & Pregunta principal & Factores que varían \\
\midrule
Validación & ¿Reproduce la implementación los casos con solución analítica conocida? & $n$; familias elípticas (normal, $t$); estructura lineal \\[4pt]
H1 & ¿Cómo evoluciona el sesgo/MSE de $\xi_n$ con $n$? & $n \times$ régimen de $\xi_0$ \\[4pt]
H2a--d & ¿Qué efecto tienen los empates en $Y$, en $X$, en ambos, y su atenuación por agregación de ítems? & número de categorías $\times$ margen discretizado (Y/X/ambos) $\times$ $n$; $B$ desempates por conjunto de datos; número de ítems $k$ para H2d \\[4pt]
H3 & ¿Hay sensibilidad a mecanismos distribucionales no elípticos, tras calibrar a igual dependencia? & forma marginal $\times$ estructura de referencia; sub-experimento aislado de invariancia monótona \\[4pt]
H4 & ¿Qué estadístico detecta mejor cada geometría de dependencia? & estadístico $\times$ estructura $\times$ intensidad; error de tipo I verificado antes de comparar potencia \\[4pt]
H5a & ¿Cómo cambia el desempeño práctico a dependencia fija? & $n \times \xi_0$ fijo \\[4pt]
H5b & ¿Cómo cambia el umbral de detección bajo alternativas locales? & $n \times$ señal decreciente en $n$ (1-2 estructuras) \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Para cada celda relevante se calculan $\xi_n$, dCor, $\tau^\ast$, y
--- como referencias aplicadas de uso rutinario, no como competidores
generales de independencia --- Pearson y Spearman. $D$ de Hoeffding se
incluye como diagnóstico histórico/complementario en H4 más que como
competidor principal: $\tau^\ast$ fue diseñado específicamente para
corregir un punto ciego conocido de $D$ (Sección~\ref{sec:framework}),
por lo que, si el presupuesto computacional obliga a priorizar, se
prioriza $\tau^\ast$ sobre $D$ antes que reducir el número de
réplicas Monte Carlo.

\subsection{Mecanismos generadores de datos}

\subsubsection{Estructura de dependencia (H4)}

Para una marginal de $X$ dada (H3) y un nivel de ruido $\sigma \ge 0$,
generamos $Y$ según el modelo aditivo $Y = f(X) + \sigma E$, con $E$
independiente de $X$ y de varianza unitaria (su distribución sigue la
misma familia asignada a la marginal de $X$ en esa condición, salvo
para la estructura circular y heterocedástica, ver abajo). Las cinco
formas funcionales $f$ son:

\begin{align*}
\text{Lineal:} &\quad f(X) = X, \\
\text{Cuadrática:} &\quad f(X) = X^2, \\
\text{Senoidal:} &\quad f(X) = \sin(2\pi X), \\
\text{Circular:} &\quad Y = Z\sqrt{1-X^2} + \sigma E, \quad Z = \pm 1 \text{ con prob. } 1/2 \text{ (independiente de $X$)}, \\
\text{Heterocedástica:} &\quad Y = 3\big(s(X)(1-\lambda)+\lambda\big)E, \quad s(X) = \mathbf{1}(|X|\le 0.5),
\end{align*}

Cada estructura tiene un análogo intuitivo en fenómenos psicológicos
reales --- mencionado aquí únicamente como ayuda interpretativa para
el lector, sin que modifique el mecanismo generador ni represente una
afirmación sustantiva sobre ningún constructo en particular:

\begin{description}
\item[Lineal] --- análoga a una relación dosis-respuesta simple, p.\ ej.\ entre una intervención y la reducción de un síntoma.
\item[Cuadrática] --- análoga a fenómenos con un punto óptimo, p.\ ej.\ activación/desempeño (ley de Yerkes--Dodson), donde tanto muy poca como demasiada activación reducen el desempeño.
\item[Senoidal] --- análoga a fenómenos cíclicos, p.\ ej.\ variación anímica circadiana o estacional.
\item[Circular] --- análoga a una relación de intensidad sin dirección preferida, p.\ ej.\ la magnitud de una respuesta emocional sin importar su valencia (positiva o negativa).
\item[Heterocedástica] --- análoga a una mayor variabilidad de respuesta bajo ciertas condiciones, p.\ ej.\ más dispersión en el afecto reportado bajo estrés alto que bajo estrés bajo.
\end{description}

Solo las estructuras circular y heterocedástica requieren que $X$
tenga soporte acotado --- para que $\sqrt{1-X^2}$ y el indicador
$\mathbf{1}(|X|\le 0.5)$ estén bien definidos ---, \emph{no} las cinco
estructuras por igual. Para estas dos estructuras específicamente, $X$
se obtiene aplicando a la variable latente $Z$ (de la familia marginal
asignada por H3) una transformación monótona acotada, $X := 2F_Z(Z)-1
\in (-1,1)$, que preserva el orden de $Z$; la ``forma marginal''
comparada en H3 se refiere, en estos dos casos, a la de $Z$ antes de
esa transformación, no a la de $X$ ya observada y acotada. Para las
estructuras lineal, cuadrática, y senoidal --- la única familia de
estructuras que H3 usa como referencia, Sección~\ref{sec:results} ---
$X$ conserva directamente su familia marginal original, sin
reescalamiento forzado, de modo que ``normal'', ``$t$'', etc.\ se
refieren en esos casos a la distribución efectivamente observada. Las
formas circular y heterocedástica siguen las alternativas de
\citet[Sección 4.3]{chatterjee2021}, usadas allí precisamente para
exhibir condiciones donde $\xi_n$ es \emph{menos} potente que dCor
(H4); las tres primeras siguen a \citet{dalitz2024} y \citet{gao2024}.

\subsubsection{Calibración del régimen de dependencia (H1, H5a)}

El nivel de ruido $\sigma$ no tiene el mismo significado para $\xi$ en
cada estructura funcional --- un mismo $\sigma$ puede corresponder a
valores muy distintos de $\xi(X,Y)$ según la forma de $f$. Por ello,
para cada combinación de (estructura, forma marginal), $\sigma$ se
calibra numéricamente, no se fija a priori, sujeto a tres
precauciones.

\emph{Monotonía no garantizada.} Una búsqueda por bisección presupone
que $\xi(X,Y)$ crece monótonamente al disminuir $\sigma$; esto no está
garantizado para todas las estructuras, en particular la circular, la
heterocedástica, o tras discretizar. Antes de adoptar la bisección
como procedimiento general, se verifica empíricamente --- para cada
combinación relevante de estructura y forma marginal --- la curva
$\sigma \mapsto \xi(X,Y)$ sobre una malla de valores; si la curva no
es monótona en el rango relevante, se sustituye la bisección por una
búsqueda global sobre esa malla.

\emph{Escala de calibración.} Para las condiciones de H2 (variables
discretizadas), la calibración se realiza sobre la variable latente
continua, \emph{antes} de discretizar --- no sobre la variable
observada. Esto permite estudiar cuánto distorsiona la discretización
una dependencia subyacente común; el valor de $\xi$ resultante en la
escala observada (tras discretizar) se calcula y reporta por separado,
no se impone como objetivo de calibración.

\emph{Sin referencia poblacional compartida entre estadísticos.} $\xi_n$,
dCor, y $\tau^\ast$ no estiman el mismo funcional poblacional ni
comparten escala, por lo que calibrar a un valor $\xi_0$ fija el nivel
de dependencia (aproximadamente) para $\xi_n$, pero no garantiza un
nivel comparable para dCor o $\tau^\ast$ bajo la misma condición. En
consecuencia (Sección~\ref{sec:design}, 3.3), el sesgo y MSE de cada
estadístico se reportan respecto de su \emph{propio} valor poblacional
--- aproximado por el mismo procedimiento Monte Carlo de gran tamaño
de muestra ($n=10^5$; usando la fórmula cerrada de
\citet{ansari2026} como atajo y verificación para las formas elípticas
de H3) --- y la comparación \emph{entre} estadísticos se limita a
cantidades comparables entre escalas: error de tipo I, potencia,
tamaño de efecto estandarizado, y tiempo de cómputo.

El valor de $\sigma$ resultante (y, si aplica, el $\xi$ observado
tras discretizar) para cada celda del diseño se documenta en el
repositorio de reproducibilidad (Sección~\ref{sec:design}, 3.4) junto
con el script de calibración.

\subsubsection{Discretización, desempate anidado, y puntaje compuesto (H2a--d)}

Partiendo de la variable latente continua generada según lo anterior,
las versiones binaria y ordinales (Likert-3, Likert-5, Likert-7) se
obtienen aplicando la función de distribución acumulada teórica de la
marginal correspondiente para cortar el rango en $c \in \{2,3,5,7\}$
categorías de igual probabilidad, asignando códigos enteros
$1,\dots,c$. Esto preserva el \emph{mecanismo} de dependencia latente
--- no necesariamente el mismo valor \emph{observado} de $\xi$, que
puede cambiar al discretizar (Sección~\ref{sec:framework}, H2). La
discretización se aplica, según el módulo, a $Y$ (H2a), a $X$ (H2b), o
a ambos (H2c), precisamente para no confundir el efecto de empates en
el covariable con el efecto de empates en la respuesta.

Para las condiciones con empates en $X$ (H2b, H2c), una sola
replicación Monte Carlo por conjunto de datos no basta, porque
$\xi_n$ depende del desempate aleatorio. Se usa en su lugar un diseño
anidado: (i) se genera un conjunto de datos latente; (ii) se
discretiza; (iii) se calcula $\xi_n$ bajo $B$ desempates aleatorios
distintos del mismo conjunto discretizado; y (iv) se descompone la
varianza total de $\xi_n$ en una componente \emph{entre} conjuntos de
datos (variabilidad de muestreo ordinaria) y una componente
\emph{dentro} de cada conjunto (variabilidad inducida por el
desempate). Esta descomposición --- planificada como resultado propio
en la Sección~\ref{sec:results} --- permite distinguir la inestabilidad
propia del muestreo de la inestabilidad introducida por la
implementación del estadístico ante empates.

Para H2d, un rasgo latente $\theta$ genera $k=10$ ítems Likert de 6
puntos, $I_j = \mathrm{discretizar}(\theta + \epsilon_j, 6)$ para
$j=1,\dots,k$, con $\epsilon_j$ ruido de medición independiente entre
ítems (mismo procedimiento de discretización que H2a--c); el puntaje
observado es $\bar{I} = \frac{1}{k}\sum_j I_j$. Los parámetros
($k=10$, escala 1--6) se eligen para coincidir con la longitud típica
de una faceta del \texttt{bfi} (Sección~\ref{sec:realdata}). Esta es
deliberadamente una simplificación de la práctica psicométrica real,
donde suele haber muchos más ítems (p.\ ej.\ 40) agrupados en varios
factores relacionados teóricamente entre sí, de modo que las
relaciones entre los puntajes compuestos resultantes son complejas
precisamente por la varianza compartida entre factores correlacionados
--- exactamente el tipo de fenómeno multifactorial difícil de aislar
que motiva este estudio (Sección~\ref{sec:introduction}). H2d se
limita a mostrar cómo la sola agregación de ítems --- sin todavía esa
complejidad multifactorial adicional --- ya atenúa el problema de
empates de H2a--c; explorar la estructura multifactorial completa
excede el alcance de este artículo (Sección~\ref{sec:introduction}).

\subsubsection{Invariancia ante transformaciones monótonas (sub-experimento de H3)}

Para aislar limpiamente la propiedad de invariancia discutida en H3
--- distinta de comparar familias marginales generadas por modelos
aditivos separados (Sección~\ref{sec:framework}) --- se ejecuta un
sub-experimento independiente del módulo H3 principal: se genera una
única distribución conjunta base $(X,Y)$, se le aplican
transformaciones estrictamente monótonas (pero no lineales) a una o
ambas marginales, y se compara la estabilidad de $\xi_n$, $\rho$ de
Spearman, y $\tau$ de Kendall (que deberían permanecer invariantes)
frente a la deriva numérica esperada en dCor (que no lo es). Este
sub-experimento, al mantener fija la distribución conjunta subyacente
y variar solo la parametrización, prueba la propiedad matemática de
invariancia de forma mucho más directa que el factorial principal de
H3.

\subsubsection{Alternativas locales e independencia exacta (H5b)}

Para el módulo H5b se seleccionan una o dos estructuras del módulo H4
(lineal y senoidal) y se define, siguiendo el marco de
\citet{auddy2024}, una secuencia de alternativas cuya señal decrece
con $n$ a una tasa explícita --- p.\ ej.\ $c_n \propto n^{-1/4}$ y
$c_n \propto n^{-1/2}$ --- en vez de un nivel de dependencia fijo,
precisamente para poder observar si el umbral de detección empírico de
$\xi_n$ se asemeja más a uno u otro orden. Para la evaluación del
error de tipo I en H1/H5a se genera, además, una condición explícita
de independencia exacta ($X \perp Y$, generada directamente sin pasar
por un $\sigma$ grande), reservando la señal decreciente en $n$ de
H5b para el estudio de potencia local cerca de independencia.

\subsection{Métricas de resultado}

Para cada condición, replicada $R$ veces:

\begin{description}
\item[Sesgo y MSE] de cada estadístico respecto a \emph{su propio} valor
  poblacional teórico --- $\xi$ para $\xi_n$, la dCor poblacional para
  dCor, y así sucesivamente (Sección~\ref{sec:design}, 3.2) --- nunca
  respecto a una única ``referencia poblacional'' compartida entre
  estadísticos, ya que no estiman el mismo funcional ni comparten
  escala (H1, H3).
\item[Descomposición de varianza] entre conjuntos de datos y dentro de
  cada conjunto por desempate aleatorio, para las condiciones con
  empates en el covariable (H2b, H2c).
\item[Error de tipo I empírico] al nivel nominal (p.\ ej.\ $0.05$),
  evaluado \emph{únicamente} bajo la condición de independencia exacta
  ($X \perp Y$ generada directamente, H1, H5a) --- nunca bajo
  alternativas, locales o no, ya que ahí la proporción de rechazos es
  potencia y no error de tipo I. Se documenta para cada estadístico el
  número de permutaciones (si aplica), la regla exacta de cálculo del
  valor $p$, y el tratamiento de empates.
\item[Potencia], evaluada bajo dependencia fija (H5a, $\xi_0>0$) y bajo
  la secuencia de alternativas locales (H5b), reportada \emph{solo}
  entre estadísticos cuyo error de tipo I esté razonablemente
  controlado bajo la misma condición --- de lo contrario la
  comparación de potencia es un artefacto de calibración, no una
  comparación real (H4). La potencia es comparable entre $\xi_n$,
  dCor, y $\tau^\ast$ porque los tres se leen como probabilidad de
  rechazo bajo una misma condición generadora, no porque los
  coeficientes compartan escala o funcional poblacional (Sección
  \ref{sec:design}, 3.2).
\item[Tiempo de cómputo], dado que la ventaja de velocidad de $\xi_n$
  es una de sus propiedades más citadas (Sección~\ref{sec:framework},
  Tabla~\ref{tab:comparison}).
\end{description}

Además de las tablas de la Sección~\ref{sec:results}, se planean
figuras que hagan visibles las interacciones entre factores: curvas de
sesgo/MSE de $\xi_n$ frente a $n$ (una curva por nivel de $\xi_0$),
curvas de potencia frente a intensidad de dependencia por estructura,
error de tipo I por tipo de variable y número de categorías,
descomposición de varianza (muestreo vs.\ desempate), tiempo de
cómputo en escala logarítmica frente a $n$, y las curvas de
calibración $\sigma \mapsto \xi(X,Y)$ por estructura (Sección
\ref{sec:design}, 3.2) que documentan la verificación de monotonía.

\subsection{Software y reproducibilidad}

Toda la simulación se implementa en \proglang{R} (versión $\ge 4.3$),
usando los paquetes \pkg{XICOR} \citep[para $\xi_n$; el mismo paquete
usado por][]{chatterjee2021,dalitz2024}, \pkg{energy} \citep[para
dCor; de los mismos autores que la propusieron,][]{szekely2007},
\pkg{TauStar} (para $\tau^\ast$, priorizado como competidor principal
de tipo rangos/cópula, Sección~\ref{sec:design}, 3.1), \pkg{Hmisc}
(para $D$ de Hoeffding, como diagnóstico complementario), y las
funciones base de \proglang{R} para Pearson y Spearman (referencias
aplicadas de uso rutinario).

\emph{Alcance computacional.} El diseño por módulos (Sección
\ref{sec:design}, 3.1) evita deliberadamente un factorial completo
--- que con los ejes considerados superaría con facilidad varios
miles de celdas --- dado que dCor, $D$, y $\tau^\ast$ tienen costo
$O(n^2)$ o peor y sus pruebas de independencia requieren permutación;
el propio \citet{chatterjee2021} reporta pruebas competidoras que no
llegaron a converger en tamaños de muestra grandes. En su lugar, cada
módulo (Tabla~\ref{tab:modules}) fija su propio conjunto mínimo de
celdas necesarias para responder su pregunta. El número de réplicas
$R$ efectivamente usado varía por módulo según el costo computacional
de la prueba involucrada --- del orden de $R=1000$ para los módulos
que solo requieren el test asintótico propio de $\xi_n$ (H1, H2, H5),
siguiendo el orden usado por \citealp{gao2024} y \citealp{dalitz2024},
y $R=300$--$500$ donde se computan además pruebas de permutación más
costosas (H3, H4) --- y se reporta explícitamente en la tabla de
resultados correspondiente en cada caso, junto con $B=20$ desempates
adicionales por conjunto de datos en los módulos con empates en el
covariable (H2b, H2c). Interacciones de orden superior entre módulos,
más allá de las planeadas, quedan como extensión posible del
repositorio de reproducibilidad (Sección~\ref{sec:discussion}).

\emph{Política de semilla.} Cada celda se ejecuta con una semilla fija
y documentada (\texttt{set.seed()}), siguiendo el mismo patrón ya
usado en las validaciones Monte Carlo de AssumptionsLab: semilla fija,
script versionado, y resultados crudos (\texttt{.csv}) committeados
junto al script que los produjo, para que cualquier lector pueda
reproducir exactamente los mismos valores.

\emph{Repositorio de reproducibilidad.} El código completo (mecanismos
generadores de datos, calibración de $\sigma$, cómputo de los tres
estadísticos, y generación de las Tablas~\ref{tab:h1}--\ref{tab:h5b})
se publica en un repositorio público dedicado,
\url{https://github.com/ArchieJamDev/ChatterjeeXi-Simulation}, bajo
\texttt{simulation/}, con su propio \texttt{README} de instrucciones
de ejecución --- separado del código fuente del manuscrito
(\texttt{paper/}) en el mismo repositorio.

\section{Resultados} \label{sec:results}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles en la version final antes de enviar.
%
% Cada tabla resume el modulo correspondiente de la Tabla de modulos
% (Seccion 3.1), manteniendo los factores no listados fijos en un
% nivel de referencia (indicado en cada caption). Interacciones de
% orden superior quedan como material suplementario/apendice
% reproducible.

\subsection{Validación}

Antes de interpretar cualquier resultado sustantivo, se verifica que
la implementación reproduzca los casos con solución analítica
conocida: el valor exacto de $\xi(X,Y)$ para la familia normal
equicorrelada de \citet[Ec.~33]{ansari2026}, $\xi(X,Y) = \frac{3}{\pi}
\arcsin\!\big(\tfrac{1+\rho^2}{2}\big) - \tfrac{1}{2}$ para $(X,Y)$
normal bivariada estándar con correlación $\rho$. Para el modelo
lineal $Y = X + \sigma E$ con $X, E \sim N(0,1)$ independientes
($\rho = 1/\sqrt{1+\sigma^2}$), la Tabla~\ref{tab:validation} compara
ese valor exacto con la estimación Monte Carlo de gran tamaño de
muestra ($n=10^5$) de nuestra propia implementación. La diferencia
máxima observada es $0.002$, por debajo del umbral de tolerancia
--- el gate de validación \emph{se supera} para la familia normal.
Una revisión más cuidadosa de \citet[Sección 5.1]{ansari2026} muestra
que, a diferencia del caso normal, esa sección no provee una fórmula
cerrada análoga para la familia $t$ de Student en general --- solo
demuestra continuidad para toda la clase elíptica, sin un valor
numérico de referencia contra el cual comparar. Además, la condición
$t$ de nuestro propio diseño ($X, E \sim t_{\text{df}}$ independientes,
$Y=X+\sigma E$; Sección~\ref{sec:design}) no corresponde a la
construcción elíptica bivariada que ese teorema exige --- una suma de
dos variables $t$ independientes no es, en general, ella misma
$t$-distribuida como par $(X,Y)$. Por tanto, la validación de la
familia $t$ contra una fórmula cerrada, tal como se planteó
originalmente, no es posible con este diseño, y se retira esa
expectativa: la condición $t$ de la Tabla~\ref{tab:h3} se reporta con
el mismo estatus epistémico que las familias no elípticas (uniforme,
exponencial, mixtura) --- consistencia interna de la simulación, sin
verificación externa contra un valor exacto conocido.

\begin{table}[H]
\caption{\label{tab:validation}Validación: $\xi(X,Y)$ exacto (normal equicorrelada, \citealp{ansari2026}) vs.\ estimación Monte Carlo ($n=10^5$) de esta implementación, modelo lineal $Y=X+\sigma E$.}
\begin{center}
\small
\begin{tabular}{ccccc}
\toprule
$\sigma$ & $\rho$ & $\xi$ exacto & $\xi$ estimado (MC) & Diferencia abs.\ \\
\midrule
3.00 & 0.316 & 0.056 & 0.057 & 0.001 \\
2.00 & 0.447 & 0.114 & 0.116 & 0.002 \\
1.50 & 0.555 & 0.180 & 0.181 & 0.000 \\
1.00 & 0.707 & 0.310 & 0.309 & 0.001 \\
0.70 & 0.819 & 0.445 & 0.447 & 0.002 \\
0.50 & 0.894 & 0.569 & 0.570 & 0.001 \\
0.30 & 0.958 & 0.725 & 0.725 & 0.001 \\
0.15 & 0.989 & 0.858 & 0.859 & 0.001 \\
0.05 & 0.999 & 0.952 & 0.952 & 0.000 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{H1: efecto del tamaño de muestra}

\begin{table}[H]
\caption{\label{tab:h1}Sesgo de muestra finita de $\xi_n$ en función de $n$, con la cota teórica de \citet{dalitz2024} como referencia, cruzado con régimen de dependencia (moderado $\xi_{\text{verdadero}}\approx 0.493$ vs.\ alto $\xi_{\text{verdadero}}\approx 0.844$), para probar directamente si el sesgo es más marcado cuando $\xi$ es alto. Referencia fija: forma = normal, tipo de variable = continua, estructura = lineal ($R=1000$ réplicas por celda; SE = error estándar Monte Carlo del sesgo, $\mathrm{sd}(\hat\xi_n)/\sqrt{R}$).}
\begin{center}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccccc}
\toprule
 & $n=20$ & $n=50$ & $n=100$ & $n=500$ & $n=2000$ & $n=2800$ & $n=3500$ \\
\midrule
Cota teórica $(n-2)/(n+1)$ & 0.857 & 0.941 & 0.970 & 0.994 & 0.999 & 0.999 & 0.999 \\
\midrule
\multicolumn{8}{l}{\emph{Régimen moderado ($\xi_{\text{verdadero}}\approx 0.493$)}} \\
Media $\hat\xi_n$ & 0.414 & 0.463 & 0.479 & 0.491 & 0.493 & 0.493 & 0.494 \\
Sesgo (SE)  & $-0.0786$ (0.0040) & $-0.0302$ (0.0024) & $-0.0138$ (0.0018) & $-0.0017$ (0.0008) & $-0.0002$ (0.0004) & $-0.0003$ (0.0003) & $0.0006$ (0.0003) \\
MSE               & 0.0221 & 0.0068 & 0.0033 & 0.0006 & 0.0002 & 0.0001 & 0.0001 \\
\midrule
\multicolumn{8}{l}{\emph{Régimen alto ($\xi_{\text{verdadero}}\approx 0.844$)}} \\
Media $\hat\xi_n$ & 0.721 & 0.798 & 0.823 & 0.841 & 0.843 & 0.843 & 0.844 \\
Sesgo (SE)  & $-0.1225$ (0.0017) & $-0.0459$ (0.0009) & $-0.0213$ (0.0006) & $-0.0035$ (0.0003) & $-0.0007$ (0.0001) & $-0.0007$ (0.0001) & $-0.0003$ (0.0001) \\
MSE               & 0.0178 & 0.0029 & 0.0009 & 0.0001 & 0.00002 & 0.00001 & 0.00001 \\
\bottomrule
\end{tabular}%
}
\end{center}
\end{table}

El sesgo se reduce monótonamente con $n$ en ambos regímenes, como
predice H1. Notablemente, a $n=20$ la cota teórica ($0.857$) está por
encima de ambos valores verdaderos y por tanto no restringe
activamente al estimador en ninguno de los dos --- y aun así se
observa un sesgo sustancial en los dos casos. Esto confirma la
reformulación condicional de H1: el sesgo de muestra finita no depende
únicamente del techo algebraico de \citet{dalitz2024}, que solo opera
cerca de $\xi=1$; existe un mecanismo de sesgo adicional incluso en
regímenes de dependencia moderada.

La comparación entre regímenes confirma además, ahora sí de forma
directamente verificable, la parte de H1 que predecía una reducción
particularmente marcada cuando $\xi$ es alto: en todos los tamaños de
muestra hasta $n=2800$, el sesgo del régimen alto es mayor en
magnitud que el del régimen moderado ($-0.1225$ vs.\ $-0.0786$ en
$n=20$, una diferencia de más del $50\%$; el patrón se mantiene sin
excepción hasta $n=2800$). En $n=3500$, sin embargo, ambos sesgos son
extremadamente pequeños ($-0.0003$ en el régimen alto vs.\ $0.0006$ en
el moderado) y la comparación cambia de signo --- una diferencia sin
relevancia práctica dado lo diminuto de ambos valores, pero que debe
reportarse con precisión en vez de generalizarse como válida ``en
cada'' tamaño de muestra. El error estándar Monte Carlo permite además
una lectura
más precisa de qué tan lejos de cero está el sesgo en cada celda: en
el régimen moderado, el sesgo en $n=2000$--$3500$ está dentro de
$1$--$2$ errores estándar de cero (genuinamente indistinguible de
cero dentro del error de esta simulación), pero en el régimen alto el
sesgo en esos mismos tamaños de muestra ($-0.0007$, SE $= 0.0001$)
sigue siendo varias veces su propio error estándar --- pequeño en
magnitud absoluta, pero no todavía indistinguible de cero. El régimen
alto no solo parte de un sesgo mayor: tarda más en desvanecerse
dentro del ruido de muestreo.

Se extendió el barrido a $n=2800$ y $n=3500$ --- tamaños frecuentes en
investigación con encuestas de gran escala, y que incluyen
deliberadamente el tamaño exacto de \texttt{bfi}
(Sección~\ref{sec:realdata}) --- para responder directamente a la
pregunta de qué ocurre más allá de $n=2000$; en el régimen moderado
(el relevante para interpretar la Sección~\ref{sec:realdata}, cuyos
valores estimados de $\xi_n$ son bajos a moderados, Tabla
\ref{tab:realdata}), el sesgo en $n=2800$ es indistinguible de cero
dentro del error de Monte Carlo reportado. (Aquí $n$ denota siempre el
número de \emph{sujetos} u observaciones $(X_i,Y_i)$, una dimensión
distinta e independiente del número de ítems $k$ que se promedian por
sujeto en H2d --- $n=2800$ son 2800 personas, no 2800 respuestas a
ítems.)

\subsection{H2a--d: empates en la respuesta, el predictor, ambos, y el puntaje compuesto}

\begin{table}[H]
\caption{\label{tab:h2a}H2a: varianza empírica y potencia de $\xi_n$ al discretizar únicamente $Y$ ($X$ continua). Referencia fija: $n=100$, forma = normal, estructura = lineal, régimen = moderado ($\xi_{\text{verdadero}} \approx 0.493$, $R=1000$ réplicas).}
\begin{center}
\begin{tabular}{lccccc}
\toprule
 & Continua & Binaria & Likert-3 & Likert-5 & Likert-7 \\
\midrule
Varianza empírica & 0.0030 & 0.0107 & 0.0060 & 0.0043 & 0.0036 \\
Potencia          & 1.000  & 0.999  & 1.000  & 1.000  & 1.000  \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

La varianza empírica confirma H2a con claridad: discretizar $Y$ la
más que triplica respecto al caso continuo cuando solo hay dos
categorías (binaria), y decrece monótonamente a medida que aumentan
las categorías, acercándose de nuevo al nivel continuo (Likert-7). La
potencia, en cambio, no discrimina en esta celda de referencia ---
está saturada cerca de $1$ en las cinco condiciones, porque a $n=100$
y un régimen de dependencia moderado ($\xi \approx 0.49$) el test
tiene potencia casi perfecta independientemente de la discretización.
El costo de los empates en la respuesta se manifiesta aquí en la
varianza del estimador puntual, no en la potencia del test; esta
última podría discriminar en regímenes de dependencia más débiles o
tamaños de muestra menores (H5a).

\begin{table}[H]
\caption{\label{tab:h2b}H2b: descomposición de varianza de $\xi_n$ al discretizar únicamente $X$ ($Y$ continua). Referencia fija: $n=100$, forma = normal, estructura = lineal, régimen = moderado ($\xi_{\text{verdadero}} \approx 0.493$; $R_{\text{externo}}=300$ conjuntos de datos, $B=20$ desempates por conjunto).}
\begin{center}
\begin{tabular}{lccccc}
\toprule
 & Continua & Binaria & Likert-3 & Likert-5 & Likert-7 \\
\midrule
Varianza entre conjuntos de datos & 0.0034 & 0.0021 & 0.0022 & 0.0024 & 0.0026 \\
Varianza dentro (desempate)       & 0.0000 & 0.0017 & 0.0013 & 0.0011 & 0.0009 \\
Potencia                          & 1.000  & 0.998  & 1.000  & 1.000  & 1.000  \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

La descomposición confirma H2b de forma limpia. Por construcción, la
varianza dentro es exactamente cero para la condición continua (sin
empates que desempatar), y crece a $0.0017$ para la binaria ---
prácticamente del mismo orden de magnitud que la varianza entre
conjuntos de datos ($0.0021$) --- lo que significa que, con solo dos
categorías, casi la mitad de la variabilidad total de $\xi_n$ proviene
únicamente de cómo la implementación rompe los empates al azar, no de
la muestra en sí. Esta componente decrece monótonamente al aumentar
las categorías ($0.0013 \to 0.0011 \to 0.0009$), acercándose al límite
continuo, exactamente como predice H2b. Como en H2a, la potencia
permanece saturada en esta celda de referencia y no discrimina entre
condiciones.

\begin{table}[H]
\caption{\label{tab:h2c}H2c: varianza empírica y potencia de $\xi_n$ con $X$ e $Y$ discretizadas conjuntamente (mismo número de categorías en ambas). Referencia fija: $n=100$, forma = normal, estructura = lineal, régimen = moderado ($\xi_{\text{verdadero}} \approx 0.493$; $300 \times 20$ réplicas anidadas).}
\begin{center}
\begin{tabular}{lccccc}
\toprule
 & Continua & Binaria & Likert-3 & Likert-5 & Likert-7 \\
\midrule
Varianza empírica & 0.0034 & 0.0124 & 0.0064 & 0.0045 & 0.0040 \\
Potencia          & 1.000  & 0.980  & 1.000  & 1.000  & 1.000  \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

H2c se confirma con claridad: la varianza en la condición binaria
conjunta ($0.0124$) supera tanto a la de H2a con empates solo en $Y$
($0.0107$, Tabla~\ref{tab:h2a}) como a la varianza total de H2b con
empates solo en $X$ ($0.0021+0.0017=0.0038$, Tabla~\ref{tab:h2b}) ---
y por un margen considerable, no recuperable por una simple suma de
ambos efectos por separado, tal como anticipaba H2c. Es también en
esta celda donde, por primera vez en H2, la potencia muestra una caída
medible ($0.980$ vs.\ $1.000$ en las demás condiciones): la
combinación de empates en ambas variables es la única condición de
H2 que llega a costar potencia detectable en este régimen de
referencia, no solo precisión puntual.

\begin{table}[H]
\caption{\label{tab:h2d}H2d: $\xi_n$ sobre un ítem Likert crudo vs.\ un puntaje compuesto de $k=10$ ítems (escala 1--6, coincidente con \texttt{bfi}), mismo rasgo latente. Referencia fija: $n=100$, forma = normal, estructura = lineal, régimen = moderado ($\xi_{\text{verdadero}} \approx 0.493$, $R=1000$ réplicas).}
\begin{center}
\begin{tabular}{lcc}
\toprule
 & Ítem único (Likert-6) & Puntaje compuesto ($k=10$) \\
\midrule
Varianza empírica & 0.0055 & 0.0035 \\
Potencia          & 0.970  & 1.000  \\
Núm.\ valores observados distintos & 6.0 & 43.4 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

H2d confirma la intuición psicométrica: el puntaje compuesto de $k=10$
ítems reduce la varianza empírica en aproximadamente un 37\% respecto
al ítem único, sube la potencia de $0.970$ a $1.000$, y multiplica por
más de siete el número efectivo de valores observados distintos
($6.0 \to 43.4$, cercano al máximo teórico de $10\times(6-1)+1=51$).
Agregar ítems por sí solo --- sin ninguna estructura multifactorial
adicional --- ya atenúa sustancialmente el costo de los empates
documentado en H2a--c.

Un barrido adicional sobre el número de ítems $k \in
\{1,2,3,5,10,20,40,100,200,400\}$ (Tabla~\ref{tab:h2d-kvar}) --- este
último rango eligiéndose deliberadamente para cubrir instrumentos de
personalidad o intereses con cientos de ítems --- muestra que el
beneficio tiene rendimientos marcadamente decrecientes a partir de
$k\approx 5$--$40$: entre ese rango y $k=400$ la varianza empírica se
mantiene casi plana ($0.0029$--$0.0036$), con una reducción adicional
de apenas $\approx 19\%$ ($0.0036 \to 0.0029$) --- pequeña frente a la
reducción de $k=1$ a $k=5$, y no acompañada de un error estándar que
permita descartar que sea, en parte, ruido de Monte Carlo, pero
tampoco estrictamente nula. Este patrón es compatible con lo que
muestra la última fila: el número de valores distintos observados se
acerca asintóticamente a $n=100$ --- el número de sujetos de la
muestra --- no al rango teórico del puntaje compuesto ($5k+1$); una
vez que prácticamente cada sujeto obtiene un valor distinto, agregar
más ítems tiene cada vez menos margen para seguir reduciendo la
varianza de $\xi_n$. Esto sugiere que, para efectos de mitigar el
costo de los empates sobre $\xi_n$ específicamente, un número
moderado de ítems ($k\approx 10$) ya captura la mayor parte del
beneficio disponible; instrumentos con
muchos más ítems (300--400, como algunos inventarios de personalidad o
intereses reales) podrían justificarse por otras razones psicométricas
(confiabilidad, validez de contenido), pero no por esta.

\begin{table}[H]
\caption{\label{tab:h2d-kvar}Efecto del número de ítems $k$ sobre la varianza empírica de $\xi_n$, mismas condiciones que la Tabla~\ref{tab:h2d} ($R=500$; $n=100$).}
\begin{center}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccccccccc}
\toprule
$k$ & 1 & 2 & 3 & 5 & 10 & 20 & 40 & 100 & 200 & 400 \\
\midrule
Varianza empírica & 0.0054 & 0.0050 & 0.0047 & 0.0036 & 0.0036 & 0.0034 & 0.0036 & 0.0035 & 0.0034 & 0.0029 \\
Valores distintos & 6.0 & 11.0 & 16.0 & 25.4 & 43.3 & 62.7 & 78.2 & 90.2 & 95.0 & 97.5 \\
\bottomrule
\end{tabular}%
}
\end{center}
\end{table}

\subsection{H3: estabilidad ante la forma de la distribución}

\begin{table}[H]
\caption{\label{tab:h3}$\xi_n$ frente a dCor bajo distintas formas marginales, cada una calibrada por separado a un mismo nivel poblacional objetivo de $\xi_n$ ($\approx 0.5$; el valor poblacional de dCor bajo esa misma condición no se fuerza a coincidir, Sección~\ref{sec:design}). Referencia fija: $n=100$, tipo de variable = continua, estructura = lineal. Sesgo/varianza: $R=1000$; error de tipo I/potencia (independencia exacta vs.\ la misma condición calibrada): $R=50$, 49 permutaciones para dCor --- una $R$ deliberadamente menor por el costo computacional y la memoria de la prueba de permutación en esta máquina, por lo que el error de tipo I reportado es impreciso ($\mathrm{SE}\approx 0.03$ dado $R=50$). La columna Normal (V) es de \emph{validación} contra \citet[Ec.~33]{ansari2026}; ninguna fórmula cerrada análoga existe para $t$ con este diseño (Sección~\ref{sec:results}, Validación), por lo que esa columna se reporta con el mismo estatus que las formas no elípticas (uniforme, exponencial, mixtura) --- consistencia interna, no verificación externa. Diferencias observadas se interpretan como sensibilidad al mecanismo generador conjunto, no exclusivamente a la marginal (Sección~\ref{sec:framework}).}
\begin{center}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
 & Normal (V) & Uniforme & Exponencial & $t$, colas pesadas & Mixtura bimodal \\
\midrule
\multicolumn{6}{l}{\emph{$\xi_n$ (valor poblacional calibrado: $0.493$, $0.495$, $0.496$, $0.507$, $0.495$)}} \\
Media $\hat\xi_n$    & 0.478 & 0.484 & 0.483 & 0.492 & 0.474 \\
Sesgo                & $-0.015$ & $-0.011$ & $-0.013$ & $-0.015$ & $-0.021$ \\
Varianza             & 0.0032 & 0.0023 & 0.0038 & 0.0043 & 0.0019 \\
Error de tipo I      & 0.04 & 0.06 & 0.02 & 0.06 & 0.04 \\
Potencia             & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
\midrule
\multicolumn{6}{l}{\emph{dCor (valor poblacional bajo el mismo $\sigma$: $0.818$, $0.834$, $0.828$, $0.821$, $0.792$)}} \\
Media                & 0.816 & 0.834 & 0.829 & 0.821 & 0.793 \\
Sesgo                & $-0.002$ & $0.0003$ & $0.0007$ & $-0.0002$ & $0.001$ \\
Varianza             & 0.0012 & 0.0006 & 0.0019 & 0.0019 & 0.0006 \\
Error de tipo I      & 0.00 & 0.04 & 0.04 & 0.08 & 0.08 \\
Potencia             & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
\bottomrule
\end{tabular}%
}
\end{center}
\end{table}

Con las cinco métricas que H3 promete evaluar, el patrón es más matizado
que la comparación de valores puntuales por sí sola. En \emph{sesgo}, es
dCor quien resulta comparativamente más estable en magnitud absoluta
($|{-0.002}|$ a $|0.001|$) frente a $\xi_n$ ($-0.011$ a $-0.021$) ---
lo opuesto a lo que la comparación de rangos de valores puntuales
sugería. En \emph{varianza}, ambos estadísticos muestran una dispersión
comparable entre familias ($\xi_n$: $0.0019$--$0.0043$; dCor:
$0.0006$--$0.0019$), sin una asimetría tan marcada como la de los
valores puntuales. En \emph{potencia}, ninguno de los dos discrimina en
esta celda de referencia --- ambos saturados en $1.00$ para las cinco
familias, igual que en H2a--c, porque $n=100$ y $\xi\approx 0.5$ es un
régimen de dependencia demasiado fuerte para que la potencia sea
informativa aquí. El error de tipo I de ambos se mantiene
razonablemente cerca del nivel nominal dado lo impreciso de su
estimación con $R=50$.

El hallazgo inicial --- que el \emph{rango de valores puntuales
estimados} de $\xi_n$ entre familias ($0.018$) es menos de la mitad
que el de dCor ($0.042$), Tabla~\ref{tab:h3} --- sigue siendo
válido como observación descriptiva, pero no debe generalizarse a
``$\xi_n$ es más estable que dCor en sesgo, varianza, y potencia'':
en sesgo específicamente, el patrón se invierte. La lectura más precisa
es que $\xi_n$ y dCor exhiben perfiles de estabilidad distintos y no
uniformemente ordenables entre sí --- consistente con el matiz que ya
reveló H4 sobre dCor y $\tau^\ast$ (Tabla~\ref{tab:h4}): ninguna medida
domina a las demás en todas las dimensiones de comparación
simultáneamente. La prueba más limpia y menos ambigua de la propiedad
de invariancia de $\xi_n$ en sí misma --- que si se sostiene sin
matices --- es el sub-experimento aislado que sigue, no esta
comparación entre familias generadoras distintas.

\begin{table}[H]
\caption{\label{tab:h3invar}Sub-experimento aislado: estabilidad de cada medida ante transformaciones monótonas de $X$ aplicadas a una misma distribución conjunta base (lineal, normal, $n=200$, $R=300$). Transf.\ 1 $=X^3$; Transf.\ 2 $=\exp(X)$.}
\begin{center}
\begin{tabular}{lccc}
\toprule
 & Sin transformar & Transf. monótona 1 & Transf. monótona 2 \\
\midrule
$\xi_n$    & 0.4848 & 0.4848 & 0.4848 \\
Spearman   & 0.8380 & 0.8380 & 0.8380 \\
Kendall    & 0.6490 & 0.6490 & 0.6490 \\
dCor       & 0.8146 & 0.7006 & 0.7574 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

El resultado es contundente: $\xi_n$, Spearman, y Kendall dan
\emph{exactamente} el mismo valor (hasta la precisión reportada) bajo
las tres transformaciones --- consecuencia directa y esperada de que
los tres son estadísticos basados en rangos, no una hipótesis a
verificar sino una propiedad matemática que esta simulación
simplemente ilustra. dCor, en cambio, varía sustancialmente
($0.701$ a $0.815$, un rango de $0.114$) pese a que la dependencia
subyacente entre $X$ e $Y$ no cambió en absoluto entre columnas ---
solo la parametrización observada de $X$. Este sub-experimento aísla
la propiedad de invariancia de forma mucho más limpia que la Tabla
\ref{tab:h3}, que compara familias generadoras distintas y por tanto
no atribuye la diferencia únicamente a la invariancia ante
transformaciones monótonas.

\subsection{H4: potencia según geometría de la dependencia}

\begin{table}[H]
\caption{\label{tab:h4}Error de tipo I y potencia de detección por estadístico y estructura de dependencia (geometría). Referencia fija: $n=100$, forma = normal, tipo de variable = continua; cada estructura calibrada por separado a $\xi_n \approx 0.10$ (el máximo común alcanzable, dado el techo estructural de heterocedástica $\approx 0.124$; $R=300$, 99 permutaciones). El error de tipo I se calcula una sola vez bajo independencia exacta y es el mismo para las cinco columnas.}
\begin{center}
\small
\begin{tabular}{lccccc}
\toprule
 & Lineal & Cuadrática & Senoidal & Circular & Heterocedástica \\
\midrule
Error de tipo I: $\xi_n$      & \multicolumn{5}{c}{0.033} \\
Error de tipo I: dCor         & \multicolumn{5}{c}{0.037} \\
Error de tipo I: $\tau^\ast$  & \multicolumn{5}{c}{0.050} \\
\midrule
Potencia: $\xi_n$      & 0.447 & 0.440 & 0.390 & 0.397 & 0.453 \\
Potencia: dCor         & 0.967 & 0.680 & 0.053 & 0.100 & 0.760 \\
Potencia: $\tau^\ast$  & 0.977 & 0.440 & 0.067 & 0.747 & 0.530 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Los tres estadísticos mantienen un error de tipo I razonablemente
cercano al nivel nominal ($0.05$), por lo que las comparaciones de
potencia son interpretables. El patrón confirma parcialmente H4 pero
con un matiz importante. Para relaciones lineales y heterocedásticas,
$\xi_n$ es claramente el menos potente de los tres ($0.447$ y $0.453$
frente a $\ge 0.53$ de dCor/$\tau^\ast$), como se esperaba. Para la
senoidal, en cambio, $\xi_n$ domina de forma contundente ($0.390$)
mientras dCor y $\tau^\ast$ apenas superan el nivel nominal
($0.053$ y $0.067$) --- prácticamente sin potencia alguna para detectar
esta dependencia oscilatoria. El caso circular, sin embargo, no
respalda la agrupación original de H4 de ``dCor y las medidas de
cópula'' como un bloque homogéneo frente a $\xi_n$: aquí es
$\tau^\ast$ ($0.747$) quien domina claramente, mientras dCor
($0.100$) se queda casi sin potencia --- dCor y $\tau^\ast$ no se
mueven juntos como un único competidor genérico, sino que cada uno
tiene fortalezas distintas según la geometría específica de la
dependencia, un matiz que la formulación original de H4 no
anticipaba.

\subsection{H5a: desempeño práctico a dependencia fija}

A diferencia de H1--H4, esta hipótesis es sobre una \emph{interacción}
--- el propio efecto de $n$ depende del régimen --- por lo que su
tabla es bidimensional en vez de un barrido de un solo factor.

\begin{table}[H]
\caption{\label{tab:h5a}Potencia de $\xi_n$ cruzando tamaño de muestra y régimen de dependencia poblacional $\xi_0$ fijo (incluye una condición de independencia exacta, no solo $\sigma$ grande). Referencia fija: forma = normal, tipo de variable = continua, estructura = lineal ($R=1000$).}
\begin{center}
\begin{tabular}{lccccc}
\toprule
 & $\xi_0 = 0$ (exacta) & $\xi_0\approx 0.1$ & $\xi_0\approx 0.3$ & $\xi_0\approx 0.5$ & $\xi_0\approx 0.8$ \\
\midrule
$n=20$   & 0.033 & 0.134 & 0.548 & 0.910 & 1.000 \\
$n=50$   & 0.047 & 0.274 & 0.909 & 1.000 & 1.000 \\
$n=100$  & 0.054 & 0.409 & 0.997 & 1.000 & 1.000 \\
$n=500$  & 0.059 & 0.927 & 1.000 & 1.000 & 1.000 \\
$n=2000$ & 0.033 & 1.000 & 1.000 & 1.000 & 1.000 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

El error de tipo I (columna $\xi_0=0$) se mantiene cerca del nivel
nominal en las cinco filas, confirmando que las diferencias de
potencia entre columnas no son un artefacto de mala calibración. El
patrón confirma H5a con claridad: el ritmo al que la potencia crece
con $n$ depende fuertemente del régimen. En $\xi_0\approx 0.1$
(cercano a independencia), se necesita $n=2000$ para alcanzar potencia
prácticamente perfecta, partiendo de solo $0.134$ en $n=20$ --- un
salto enorme. En $\xi_0\approx 0.8$, en cambio, la potencia ya está
saturada en $1.000$ incluso con $n=20$. Esta enorme diferencia en el
``costo de muestra'' según qué tan cerca esté la dependencia real de
cero motiva directamente el módulo H5b, que examina formalmente el
umbral de detección bajo alternativas locales en ese mismo régimen
cercano a independencia.

\subsection{H5b: sensibilidad local}

\begin{table}[H]
\caption{\label{tab:h5b}Potencia empírica bajo alternativas locales cuya señal decrece con $n$ a tasas $n^{-1/4}$ y $n^{-1/2}$ \citep{auddy2024}, estructuras lineal y senoidal. Ambas secuencias se anclan al mismo valor inicial $\xi(n_0{=}100)=0.15$, divergiendo solo en la tasa de decrecimiento. Referencia fija: forma = normal, tipo de variable = continua ($R=1000$).}
\begin{center}
\begin{tabular}{lcc}
\toprule
 & Señal $\propto n^{-1/4}$ & Señal $\propto n^{-1/2}$ \\
\midrule
$n=100$ (lineal)    & 0.728 & 0.728 \\
$n=500$ (lineal)    & 0.918 & 0.759 \\
$n=2000$ (lineal)   & 0.995 & 0.636 \\
$n=100$ (senoidal)  & 0.680 & 0.680 \\
$n=500$ (senoidal)  & 0.899 & 0.713 \\
$n=2000$ (senoidal) & 0.997 & 0.583 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

El resultado es consistente con la asimetría teórica de
\citet{auddy2024} --- no la demuestra ni estima el umbral de forma
robusta, como se precisa más abajo, pero el patrón esperado es
detectable incluso en este diseño acotado. Ambas tasas parten del
mismo punto en $n=100$ (por
construcción), pero divergen dramáticamente al crecer $n$: bajo la
tasa $n^{-1/4}$ --- el propio umbral de detección de $\xi_n$ --- la
potencia \emph{aumenta} monótonamente hasta saturarse cerca de $1$ en
$n=2000$ ($0.995$ lineal, $0.997$ senoidal), señal de que $\xi_n$
mantiene el ritmo frente a una señal que decrece a esa velocidad. Bajo
la tasa $n^{-1/2}$ --- la tasa óptima paramétrica que, según
\citet{auddy2024}, $\xi_n$ \emph{no} está garantizado a detectar ---
la potencia sube brevemente y luego \emph{cae} de manera marcada
($0.759 \to 0.636$ en lineal; $0.713 \to 0.583$ en senoidal, entre
$n=500$ y $n=2000$), abriendo una brecha de más de $0.35$ frente a la
tasa $n^{-1/4}$ en $n=2000$ pese a partir del mismo punto. Este
patrón es consistente, en una escala de muestra accesible, con la
predicción teórica de que $\xi_n$ pierde terreno frente a señales que
decrecen más rápido que su propio umbral de detección --- exactamente
el mecanismo que motiva H5b. Con solo tres tamaños de muestra y dos
estructuras, este experimento no pretende \emph{establecer} la tasa
minimax de forma robusta --- eso corresponde a la prueba teórica de
\citet{auddy2024} --- sino mostrar que la asimetría que predicen es
detectable empíricamente incluso con un diseño modesto.

\section{Ilustración con datos reales} \label{sec:realdata}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles en la version final antes de enviar. Sin
% resultados numericos todavia -- esto es el plan de analisis, a
% ejecutar una vez completada la simulacion.

Siguiendo la recomendación de AJS de ilustrar los métodos con datos
reales e interesantes, y no con ejemplos triviales, se aplica $\xi_n$
y sus medidas de comparación al conjunto de datos \texttt{bfi} del
paquete \pkg{psych} \citep{revelle2024psych}, originado en el proyecto
SAPA \citep{revelle2010sapa}: 2800 sujetos, 25 ítems de personalidad
tipo Likert (escala 1--6, cinco facetas del modelo Big Five), más edad
(continua), género (binaria), y nivel educativo (ordinal, 1--5). Se
eligió específicamente porque combina, dentro de un único conjunto de
datos real y de tamaño considerable, los tres tipos de variable del
diseño de simulación (Sección~\ref{sec:design}), permitiendo mostrar
las precauciones de interpretación de H1--H5 en un caso aplicado, no
solo en condiciones simuladas.

Se examinan tres pares ilustrativos, elegidos para representar cada
tipo de comparación de variable del diseño:

\begin{description}
\item[Continua--continua.] Edad frente a un puntaje sumario de
  neuroticismo, para explorar si la relación es genuinamente monótona
  (donde Pearson/Spearman deberían bastar) o si $\xi_n$ detecta
  estructura adicional no monótona.
\item[Ordinal--ordinal.] Dos ítems individuales de la misma faceta
  (Likert 1--6), como caso aplicado directo de H2a/H2c: se reporta
  $\xi_n$ bajo distintos desempates aleatorios para ilustrar
  empíricamente la variabilidad inducida por los empates documentada
  en H2b.
\item[Binaria--continua.] Género frente a un puntaje sumario de
  extraversión, ilustrando el caso binario de H2a.
\end{description}

Para cada par se reportan $\xi_n(X,Y)$ y $\xi_n(Y,X)$ --- dada la
direccionalidad de $\xi_n$ (Sección~\ref{sec:framework}) ---, junto
con dCor, $\tau^\ast$, Pearson, y Spearman, y se discuten las
diferencias a la luz de los hallazgos de la Sección~\ref{sec:results}:
por ejemplo, si $n=2800$ es suficientemente grande para que el sesgo
de muestra finita (H1) sea despreciable, o si el número de categorías
de los ítems Likert (H2) sugiere cautela en la interpretación puntual
de $\xi_n$ frente a su intervalo de incertidumbre.

\begin{table}[H]
\caption{\label{tab:realdata}Coeficientes calculados sobre \texttt{bfi} para los tres pares ilustrativos (casos completos por par: $n=2694$, $2757$, y $2713$ respectivamente, tras excluir valores perdidos).}
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
 & Edad -- Neuroticismo & Ítem 1 -- Ítem 2 (Likert) & Género -- Extraversión \\
 & (continua--continua) & (ordinal--ordinal) & (binaria--continua) \\
\midrule
$\xi_n(X,Y)$ & 0.009 & 0.358 & 0.024 \\
$\xi_n(Y,X)$ & 0.025 & 0.375 & 0.003 \\
dCor         & 0.109 & 0.670 & 0.098 \\
$\tau^\ast$  & 0.003 & 0.164 & 0.002 \\
Pearson      & $-0.114$ & 0.707 & 0.101 \\
Spearman     & $-0.099$ & 0.703 & 0.096 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Los tres pares ilustran hallazgos distintos de la Sección
\ref{sec:results} sobre datos reales. En Edad--Neuroticismo, Pearson y
Spearman detectan una asociación negativa modesta pero clara
($\approx -0.10$, consistente con la literatura sobre neuroticismo y
edad), y dCor una magnitud comparable de dependencia general
($0.109$); $\xi_n$, en cambio, da un valor mucho menor ($0.009$--$0.025$)
--- una réplica directa, en datos reales, del patrón de H4 (Tabla
\ref{tab:h4}): $\xi_n$ es menos sensible que Pearson/dCor ante
relaciones aproximadamente monótonas, y su valor puntual no debe
interpretarse como ``ausencia de asociación'' sin consultar además
Pearson o Spearman. En Ítem 1--Ítem 2 (dos ítems de neuroticismo),
todos los estadísticos coinciden en una asociación fuerte, como se
espera de dos indicadores del mismo constructo; la desviación estándar
de $\xi_n$ entre $20$ desempates aleatorios independientes es $0.010$
--- una réplica directa, en datos reales, de la variabilidad por
empates documentada en H2b, aunque atenuada aquí por el tamaño de
muestra considerablemente mayor ($n\approx 2757$) que las celdas de
referencia de H2 ($n=100$). En Género--Extraversión, los seis
estadísticos coinciden en una asociación muy débil o nula, un hallazgo
sustantivo razonable dado que las diferencias de género en
extraversión reportadas en la literatura suelen ser pequeñas.

\section{Discusión} \label{sec:discussion}

% NOTA: seccion redactada en español para facilitar la revision.
% Traducir al ingles en la version final antes de enviar.

\subsection{Qué se confirmó, qué se refinó, y qué fue inesperado}

Las cinco hipótesis, fundamentadas en propiedades ya demostradas de
$\xi_n$ (Sección~\ref{sec:framework}), recibieron evidencia real en
todos los casos, aunque no de forma uniformemente confirmatoria: dos
de ellas (H1, H3) requirieron ampliar el diseño original para
someterlas a prueba de forma completa, y una (H4) produjo un hallazgo
genuinamente inesperado.

H1 se confirmó en su forma condicional --- y ahora también en la
predicción específica que originalmente solo se afirmaba, sin
probarla directamente: el sesgo de muestra finita es real, decrece con
$n$, no depende únicamente de la cota algebraica de
\citet{dalitz2024} (existe sesgo sustancial incluso cuando esa cota no
restringe activamente al estimador), y es mayor en magnitud bajo un
régimen de dependencia alto que bajo uno moderado en todos los
tamaños de muestra hasta $n=2800$ --- aunque ambos sesgos son ya
extremadamente pequeños y la comparación se invierte en $n=3500$
(Tabla~\ref{tab:h1}). H2,
desagregada en H2a--d, mostró que los empates en la respuesta y en el
predictor son mecanismos distintos con consecuencias distintas:
varianza pura en un caso, varianza más aleatorización por desempate en
el otro (Tablas~\ref{tab:h2a}--\ref{tab:h2b}), y un efecto conjunto
(Tabla~\ref{tab:h2c}) que no es la suma de ambos. H2d mostró que la
práctica psicométrica estándar de agregar ítems en un puntaje
compuesto atenúa este problema, aunque con rendimientos marcadamente
decrecientes: la meseta se alcanza con relativamente pocos ítems, y
los instrumentos de cientos de ítems añaden solo una reducción
adicional modesta, porque el margen de mejora pasa a estar acotado
por el tamaño de la muestra, no por el número de ítems. H3, al evaluarse
en las cinco métricas que la propia hipótesis prometía (sesgo,
varianza, error de tipo I, y potencia, no solo el valor puntual
estimado), resultó más matizada de lo que sugería la comparación
inicial: $\xi_n$ mostró un rango menor de \emph{valores puntuales}
entre familias que dCor, pero fue dCor quien mostró menor \emph{sesgo}
en magnitud absoluta, y ambos exhibieron varianza comparable
(Tabla~\ref{tab:h3}) --- ninguna de las dos medidas domina a la otra en
todas las dimensiones simultáneamente. La propiedad de invariancia de
$\xi_n$ en sí misma, en cambio, se sostuvo sin matices en el
sub-experimento aislado que mantiene fija la distribución conjunta
base (Tabla~\ref{tab:h3invar}) --- la comparación entre familias
generadoras distintas y la invariancia ante transformaciones monótonas
resultaron ser, como advertía la reformulación de H3, preguntas
relacionadas pero no idénticas. H5a y H5b fueron consistentes, de
forma nítida para un estudio de simulación con recursos
computacionales modestos, con la asimetría teórica de
\citet{auddy2024} entre el umbral de detección $n^{-1/4}$ de $\xi_n$ y
la tasa paramétrica óptima $n^{-1/2}$ (Tabla~\ref{tab:h5b}) --- sin
que ese diseño acotado (tres tamaños de muestra, dos estructuras)
pretenda establecer esa tasa de forma robusta por sí solo.

H4 fue la hipótesis con el hallazgo más inesperado. Se confirmó el
patrón ya documentado por \citet{chatterjee2021} y \citet{gao2024}
--- $\xi_n$ menos potente que dCor y $\tau^\ast$ para relaciones
lineales y heterocedásticas, más potente para relaciones oscilatorias
--- pero la estructura circular reveló que dCor y $\tau^\ast$ no se
comportan como un bloque homogéneo de ``alternativas a $\xi_n$'': es
$\tau^\ast$, no dCor, quien domina en esa geometría particular
(Tabla~\ref{tab:h4}). Esto sugiere que la elección de punto de
comparación importa tanto como la elección de $\xi_n$ mismo, y que
recomendaciones que traten ``las medidas basadas en distancias/cópulas''
como intercambiables entre sí deben revisarse caso por caso según la
geometría de la dependencia esperada.

\subsection{Una guía práctica para investigadores en psicología}

El mensaje central de este estudio, dirigido al público que motivó su
diseño (Sección~\ref{sec:introduction}), puede resumirse así: $\xi_n$
debe interpretarse como una medida \emph{direccional} de qué tan bien
$Y$ está determinada por $X$, con ventajas específicas para ciertos
patrones funcionales y costos específicos bajo empates, discretización,
dependencia débil, y ciertas geometrías de asociación --- no como una
medida universalmente superior o inferior a Pearson, Spearman, dCor, o
$\tau^\ast$. Concretamente:

\begin{itemize}
\item Con $n$ pequeño ($<100$) y dependencia que se sospecha alta,
  esperar sesgo a la baja no despreciable (H1); con $n$ del orden de
  miles (como en \texttt{bfi}), el sesgo es prácticamente nulo.
\item Con ítems Likert individuales, especialmente de pocas categorías,
  esperar mayor varianza --- y, si el covariable es el discretizado,
  parte de esa varianza proviene solo del desempate aleatorio, no de
  la muestra (H2a--c); agregar ítems en un puntaje compuesto ayuda,
  con la mayor parte del beneficio ya lograda con relativamente pocos
  ítems (H2d).
\item Un valor bajo de $\xi_n$ no debe interpretarse como ``sin
  asociación'' sin revisar también Pearson o Spearman --- la
  ilustración de la Sección~\ref{sec:realdata} (Edad--Neuroticismo)
  muestra exactamente este escenario con datos reales.
\item Si se sospecha una relación genuinamente no monótona u
  oscilatoria, $\xi_n$ es probablemente más sensible que las
  alternativas (H4); si se sospecha una relación suave, monótona, o
  heterocedástica, dCor o $\tau^\ast$ probablemente detecten mejor.
\item Cerca de independencia, $\xi_n$ requiere tamaños de muestra
  sustancialmente mayores que en regímenes de dependencia moderada o
  alta para alcanzar potencia razonable (H5a--b) --- una consideración
  relevante al planear el tamaño muestral de un estudio.
\end{itemize}

\subsection{Limitaciones}

Como se declaró explícitamente desde la introducción, este estudio
cubre condiciones \emph{típicas}, no adversariales; los resultados de
peor caso de \citet{bucher2025} permanecen fuera de su alcance por
diseño, no por omisión. Las réplicas Monte Carlo variaron entre
módulos ($R=300$ a $1000$) según el costo computacional de cada
prueba --- las pruebas de permutación de dCor y $\tau^\ast$ en H4
son sustancialmente más costosas que la prueba asintótica propia de
$\xi_n$ usada en H1, H2, y H5 ---, por lo que la precisión de las
estimaciones no es uniforme entre tablas. La mayoría de los módulos
usa una única celda de referencia (forma normal, estructura lineal, o
$n=100$, según el módulo) para aislar el efecto de interés, siguiendo
el diseño por módulos de la Sección~\ref{sec:design}; no se exploró la
matriz factorial completa de interacciones de orden superior entre los
cinco factores, que queda como extensión posible del repositorio de
reproducibilidad. Para la familia $t$ de Student, ninguna fórmula cerrada análoga a la
normal existe en \citet{ansari2026} con este diseño (Sección
\ref{sec:results}, Validación); los resultados de H3 para esa familia
deben leerse, como los de uniforme/exponencial/mixtura, como
internamente consistentes, no como validados externamente contra un
valor exacto conocido.

\subsection{Líneas futuras}

La ceguera ante la posible no monotonía documentada en la
Introducción no se limita a los coeficientes bivariados: explorar
sistemáticamente qué ocurre cuando los coeficientes de ruta
verdaderos de un modelo de ecuaciones estructurales son en sí mismos
no monótonos --- y si diagnósticos basados en $\xi_n$ pueden detectar
esa mala especificación mejor que los diagnósticos de ajuste
habituales --- es una extensión natural de este trabajo hacia el
análisis de ruta y los modelos de segundo orden, y se deja
explícitamente como línea de investigación futura, distinta de este
artículo.

\section{Conclusión} \label{sec:conclusion}

Este artículo sometió a $\xi_n$, el coeficiente de correlación de
\citet{chatterjee2021}, a un diseño de simulación Monte Carlo
organizado por módulos y fundamentado en propiedades ya demostradas
del estimador --- no en un barrido exploratorio sin guía --- cruzando
tamaño de muestra, tipo de variable, forma de la distribución,
geometría de la dependencia, y régimen de dependencia. Confirmamos que
ninguno de estos cruces existía previamente en la literatura sobre
$\xi_n$ (Sección~\ref{sec:introduction}), y que cada uno revela un
comportamiento con consecuencias prácticas concretas para quien
analiza datos psicológicos: sesgo de muestra finita que decrece con
$n$ pero no depende solo de la cota algebraica conocida; costos
distintos y no aditivos de los empates según si afectan al predictor,
a la respuesta, o a ambos, atenuables mediante puntajes compuestos con
rendimientos decrecientes; una relación con dCor ante cambios de forma
de la distribución más matizada de lo esperado --- $\xi_n$ más
estable en el valor puntual, pero no uniformemente en sesgo o
varianza --- con la propiedad de invariancia de $\xi_n$ en sí misma
sosteniéndose sin matices en un sub-experimento aislado; una geometría de
la dependencia que determina qué estadístico detecta mejor, con dCor y
$\tau^\ast$ divergiendo entre sí de forma inesperada en el caso
circular; y una asimetría empíricamente nítida entre el umbral de
detección subóptimo de $\xi_n$ cerca de independencia y su optimalidad
minimax lejos de ella. La ilustración con datos reales de personalidad
(\texttt{bfi}) reprodujo dos de estos hallazgos directamente sobre
datos aplicados, no solo simulados. En conjunto, estos resultados
apoyan el mensaje central del estudio: $\xi_n$ es una herramienta
valiosa y direccional para detectar dependencia funcional arbitraria,
pero su uso responsable en investigación psicológica exige atender al
tamaño de muestra, al tipo de variable, y a la geometría de la
dependencia esperada --- no tratarlo como un reemplazo universal de
los coeficientes de asociación clásicos.

%\bibliographystyle{plainat}
\bibliography{paper}

\end{document}
